{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "encore run",
    "start": "encore run",
    "build": "encore build",
    "test": "vitest",
    "build:frontend": "cd ../frontend && npm install && npx vite build --outDir=../backend/frontend/dist"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "encore.dev": "^1.53.0",
    "jsonwebtoken": "^9.0.2",
    "stripe": "^17.7.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.0.9"
  },
  "engines": {
    "node": ">=18"
  }
}